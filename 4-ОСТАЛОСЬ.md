# Что осталось сделать

> ## ✅ ЦЕЛЬ 1 ДОСТИГНУТА (2026-07-11)
> **CNL — единственный формат авторинга. YAML/`​```ir`-авторинг удалён, escape hatch отсутствует.**
> Блоки A (reader-паритет, D4=close-all), B (документация CNL-only), C (удаление) — завершены.
> Полный gate зелёный по всем модулям (`./gradlew jvmTest :desktopApp:compileKotlin` → BUILD
> SUCCESSFUL). Grep: нулевые authoring-символы (`parseTypedGroup`/`typedKeyOf`/`IrEscapeHatch`/
> 8 YAML-райтеров) — остался только CNL-собственный `osaDistance`. Все 5 демо — pure CNL.
> Изменено ~95 файлов, удалено 30 (authoring-поверхности + мигрированные фикстуры). Бонус:
> реализован **section-move reorder** (z-order персистится в CNL через `MoveSection`). Открытые
> follow-up'ы (не блокеры) — внизу.

Это «вторая половина» миссии — сделать CNL достаточным и **убрать YAML/`​```ir`-авторинг**.
Грамматика нод-уровня уже готова ([3-СДЕЛАНО](3-СДЕЛАНО.md)); ниже — что не хватает.
Порядок и номера шагов (S1–S36) — из [2-ПЛАН](2-ПЛАН.md).

## Шесть блоков работы (высокоуровнево)

1. **Document-scoped авторинг CNL** — variable collections, component definitions, shared
   styles и единый document emit закрыты. (Фаза 2: S10–S18)
2. **Закрытие reader-gap'ов** — расширить `engine/ir` + readers, чтобы эмиттер round-trip'ил
   вместо `null`. (Фазы 0/1/3c)
3. **Миграция 4 YAML-демо → CNL** (emitter-driven, equivalence-gated). ✅ **ЗАВЕРШЕНО:**
   все 4 демо (`ShapesShowcase`, `MissionOverview`, `MissionTelemetry`, `MissionEventLog`)
   переведены; `grep` подтверждает нулевой `ir`/YAML-авторинг в shipped `*Slm.kt`. (Фаза 3)
4. **Write-back parity** — 3-tier `CnlWriter` через эмиттер, до удаления YAML-райтеров. (Фаза 4)
5. **Документация** — spec / SLM-SKILL / README. (Фаза 5)
6. **Удаление** authoring-поверхностей — строго последним. (Фаза 6)

---

## 1. Document-scoped авторинг (Фаза 2 — главный блокер)

`variables`, `component definitions`, `shared styles` и единый full-document emitter уже
получили CNL parse/emit. Фаза 2 закрыта как отдельный блок; дальше blocker'ы — reader gaps
и миграция демо.

- ✅ **Variable collections** → `DesignDocument.variables`. Все демо определяют коллекцию
  `theme` (modes light/dark, 12 переменных, среди них var→var alias `$color.accent`).
  Готово: ветка экстрактора (S10), parse-in (S11), emit (S12). Десугар идёт в существующий
  `VariablesBlockReader`/`VariablesPatch`/`liftDocumentPatches`.
- ✅ **Component definitions** → `DesignDocument.components`/`componentSets`. Готово:
  `Component:` heading с CNL metadata (`component-name`, `set`, `axis`, `variant`,
  `prop`) десугарится в существующий `ComponentPatch`/`ComponentLifter`; определения
  исчезают из visible tree. Добавлен отдельный `emitComponents(document)` (S13/S14).
  `componentSets` по-прежнему производные; эмиттер восстанавливает axes/variant по
  `DesignComponentSet`.
- ✅ **Shared styles** → `DesignDocument.styles`. Готово: `# Styles`, строки
  `Paint`/`TextStyle`/`Effect`/`Grid`, новый `StylesPatch`/`TypedBlockKind.Styles`/
  `readStylesBlock`, lift в `DesignDocument.styles`, `emitStyles(document)` и resolver
  fixed point. *Ни одно из 4 демо shared styles не определяет*, но путь закрыт для
  паритета/будущего.
- ✅ **Инфраструктура (S10):** document-scope/lifted branches готовы для
  `Collection`/`Prototype Variables`, `Component:` definitions и `Styles`.
  Анти-фантом-нода-гард действует для всех трёх словарей.
- ✅ **S17 — cross-check «узел → словарь»:** aggregate-тест покрывает `Collection`,
  `Styles`, `Component:`, body refs (`styles`, `text-style`, `Instance of`, `$prop.*`)
  и resolver после `emitDocument → compile`.
- ✅ **S18 — единый `emitDocument`:** standalone `.layout.md` = retained YAML frontmatter +
  variables + styles + body + component definitions. Body-only генерация вынесена в
  `emitDocumentBody`.

## 2. Reader-gap'ы (актуальный остаток после S1–S8b)

Закрыто в текущей сессии: G-TEXTKEY, G-WEIGHT, G-ADDL (`visible`/`locked`),
G-GRIDTRACKS, G-IMPLICITROWS, G-MOTIONREF, G-STROKESTYLE, G-STICKY, G-VARMODES,
а также часть G-OVR для `characters $prop.*` и node-body `visible $prop.*`.

Остались gaps, не нужные для первых трёх демо или относящиеся к более поздней миграции:

| Gap | Сложность | Суть | Статус |
|-----|-----------|------|--------|
| ~~**G-OVR / S24**~~ | ✅ done | `overrides.set` по id-пути — фраза `override <id/path> ( … )` | закрыт (S24) |
| ~~**G-WPS** (S25)~~ | ✅ done | `weight-per-side (T R B L)` в `stroke ( … )` | закрыт |
| ~~**G-STROKEPAINT** (S26)~~ | ✅ done | полный fill-paint стек в `stroke ( … )` (solid-props/gradient/image/video) | закрыт |
| ~~**G-MASK** (S29)~~ | ✅ done | `mask.source` (`from <id>`) — новое IR-поле + serialization + resolver | закрыт (авторинг-паритет) |
| ~~**G-BOOL** (S29)~~ | ✅ dropped | boolean-op id-list — отброшен (операнды = дети); `BooleanOpPatch.children` удалён | отброшен |
| ~~**G-SPANS** (S27)~~ | ✅ done | rich-text style-спаны — поле `TextStyleRange.styleRef` + serde + resolver + fingerprint; фраза `span (range (a b) style <ref>)` | закрыт (авторинг-паритет; emit в порядке IR-списка) |
| ~~**G-BIND** (S28)~~ | ✅ done | `$ref/{{expr}}` в literal-слотах — Bindable-ификация effects/grid/media + глобальный `DesignPoint` widen | закрыт (literal close-all) |

> **По D4 = «закрыть все»: 5 из 5 ЗАКРЫТО** (S25/S26/S27/S28/S29) + G-BOOL отброшен. **Reader-паритет
> завершён** — CNL на полном паритете с IR (parse/emit/write-back), «no escape hatch». Полный gate
> зелёный по всем модулям (`./gradlew jvmTest :desktopApp:compileKotlin` → BUILD SUCCESSFUL:
> engine:ir 275, engine:frontend 518, shared 218, engine:scene 25, engine:backend-compose 18,
> anchoring 43).
> S27/S28 закрыты серией workflow'ов (recon/investigation → implement → adversarial-verify);
> adversarial-проходы поймали 3 реальных дефекта (реордеринг спанов, `-0.0`-сериализация после
> widen'а, dead-binding риск grid overlay) — все исправлены; 3 обочинных edge-кейса вынесены в
> отдельные задачи (renderLinks order, fill-paint focal emit, `$`-ref/`fr` grid-track ambiguity).

### S28 — под-шаги ✅ ЗАВЕРШЕНО (D4-refine: пользователь выбрал **literal close-all**)
Разведочный workflow (template→sweep→synthesize) дал точную карту. Закрыты ВСЕ слоты, включая
глобальное расширение `DesignPoint.x/y → Bindable<Double>`. Все под-шаги: один агент → gate →
adversarial-верификация (как S27).
1. ✅ **S28a-scalars + S28c-strings** — effect `blur/spread/radius` (6) + media `assetId/posterAssetId` (2).
   Медиа-ref = ключ в `document.assets` → `resolveString` ДО lookup; `MediaAssetChecks` пропускает ref-типы.
2. ✅ **DesignPoint widen** — `DesignPoint.x/y: Double → Bindable<Double>` (secondary literal-ctor
   минимизирует churn: 11 construction + 72 read сайтов; helper `Bindable<Double>.orZero`). Adversary
   поймал `-0.0`-регрессию в omit-guard (boxed-Double total-order писал `-0.0`) → helper `differsFrom`
   на все 15 zero-guard'ов + регресс-тест.
3. ✅ **Effect-offset + media-focalPoint wiring** — `resolvePoint`/`resolveDouble` на осях (ref → значение,
   не 0.0); numberToken emit; `assertSameFields` теперь сверяет `effects`.
4. ✅ **S28b grid tracks** — `GridTrack.Fixed/Flex.value` + `implicitRowMin` + net-new `resolveTrack`
   на границе резолвера (Bindable не течёт в чистый layout-движок). Edge: `$`-ref id на «fr» ↔ Flex-суффикс
   (низкий приоритет, в задаче).
5+6. ✅ **grid overlay + count** — `LayoutGridDefinition.count` (новый lane `Bindable<Int>`:
   `bindableInt`/`readBindableInt`/`intJson`/`resolveInt`/`intToken`) + `size/gutter/margin`
   (`Bindable<Double>?`) + net-new `resolveLayoutGrid` (иначе overlay-`$var` = dead binding). Объединены,
   чтобы не шипнуть dead binding.

**Итог Block A:** CNL ↔ IR полный паритет, gate зелёный по всем модулям. Дальше — Block B (docs) → Block C (removal).

## 3. Миграция 4 демо → CNL (Фаза 3; блокируется пунктами 1 и 2)

Механика: заморозить YAML как снапшот → `emitDocument` → equivalence-тест
`compile(YAML).IR ≡ compile(CNL).IR` → заменить `*Slm`-константу. Пофайловые блокеры:

| Демо | Строк | Document-scoped | ir-only фичи | Что разблокирует (шаги) |
|------|-------|-----------------|--------------|--------------------------|
| **CnlShowcase** | ~38 | нет | нет | уже CNL — контрольный (S19) |
| **ShapesShowcase** | ~727 | `theme` | нет | ✅ S20 закрыт: shipped source CNL, YAML snapshot только в migration-тесте |
| **MissionOverview** (дефолтный экран) | ~3546 | `theme` (12 var, alias) | **нет** (плоское Free-layout: 32 frame/35 shape/66 text) | ✅ S21 закрыт: shipped source CNL, YAML snapshot только в migration-тесте |
| **MissionTelemetry** | ~368 | `theme` + **2 component def** (`cmp_wire_tile_default/highlight`) | закрыто: ref-less motion, auto-hug implicit rows, preferred `fill`/`hug` sizing values | ✅ S22 закрыт: shipped source CNL, YAML snapshot только в migration-тесте |
| **MissionEventLog** | ~376 | `theme` + **1 component def** (`cmp_log_row`, 2 text-props) | закрыто: `overrides.set` fills (row_3/5), `ir`-фенсы, footer padding, characters $prop | ✅ **S23/S24 закрыты**: shipped source CNL, YAML snapshot только в migration-тесте |
| **LegacyMissionOverviewFixture** (тест) | ~450 | `theme` + 2 component def (вкл. boolean-prop `showTail`) | осталось: `overrides.set` fill (card_2) — **теперь выразимо через `override`**; закрыто: `visible $prop.showTail` | по **D5**: (a) мигрировать (S24 готов) или (c) удалить/перевесить — **не блокирует** |

> ✅ S24 (`overrides.set`) и S23 (EventLog) закрыты. `LegacyMissionOverviewFixture` — теперь
> технически мигрируема (`override card_2 (...)`), но это тест-фикстура: решение D5 (мигрировать
> vs удалить) остаётся открытым и не блокирует Фазы 4–6.

## 4. Write-back parity (Фаза 4) — ✅ ЗАВЕРШЕНО

- ✅ **S30:** `CnlWriter` теперь трёхуровневый — tier-1 (span-replace), tier-2 (phrase-append),
  **tier-3** (re-emit всего предложения из пропатченного `DesignNode` через `CnlEmitter`).
  Пропатченный узел протянут из редьюсера (`document.nodeById(nodeId)?.let(patchNode)`).
  **Silent CNL→YAML fall-through в `SlmPatcher` убран**: CNL-узел, невыразимый даже tier-3,
  падает в in-memory, а не в YAML typed-блок.
- ✅ **S31:** `NodeSectionWriter` (structural insert) и `ScreenSourceWriter` (create-screen)
  эмитят CNL через `CnlEmitter.emitStableSubtree` (frontmatter остаётся YAML). У 8
  YAML-райтеров write-back'а structural-вызовов больше нет → удаляемы в S35.
  `headingPrefix` диспатчит по `kind` (in-tree Frame-типы → `Frame:`).

## 5. Документация (Фаза 5) — ✅ ЗАВЕРШЕНО (Block B)

Workflow (survey → 4 параллельных writer'а → consistency-review). Consistency-проход поймал 2
реальных дефекта (SLM-SKILL учил невалидной `color (gradient …)`-обёртке — парсер роняет вложенный
gradient; спека оставила 3 legacy-раздела prose-авторинга) — исправлено.
- ✅ **S32:** `design-book/semantic-layout-markdown-i18n.md` — полный CNL-справочник фраз, implicit-`order`
  задокументирован; legacy prose/pipeline/typed-blocks-as-authoring разделы переписаны на CNL-only
  (Pipeline mermaid → `compileSlm`, Source Format / Markdown Semantics → CNL-структура); невалидные
  примеры (`color (gradient …)`, bare-dotted токены без `$`) исправлены.
- ✅ **S33:** `SLM-SKILL.md` (CNL-first, bare `gradient (…)`/`image (…)` формы), `engine/README.md`
  (YAML-readers = внутренняя desugar-стадия, не авторинг), `EDITOR.md` (write-back CNL tier-1/2/3 +
  fidelity-veto), заметка в памяти. **B3:** PR #13 уже MERGED — действий не требуется.
- Остаточный follow-up (не блокер): compile-валидация ВСЕХ CNL-примеров в доках (greps поймали
  известные классы ошибок, но полная компиляция примеров не прогнана).

## 6. Удаление authoring-поверхностей (Фаза 6 — строго последним) — 🔵 В РАБОТЕ (Block C)

Предпосылка: S1–S33 зелёные ✅, все 5 демо pure-CNL (grep = 0 YAML/ir) ✅.
Investigation-workflow (parse-paths / yaml-writers / fixtures) → ordered gate-green removal plan (C1–C7).

**Решения (после разведки):**
- **D5 = удалить + принять потерю покрытия.** Shipped `MissionOverviewSlm` — wireframe без нужных
  конструкций, поэтому rewrite-in-place отклонён; `LegacyMissionOverviewFixture` удаляется, 6 тестов
  перевешиваются на shipped CNL-демо, покрытие Legacy-only (detach-instance/auto-layout/instance-
  fallback/override-set-fill/boolean-prop) теряется осознанно.
- **ir-splice тест = переэкспресс через CNL prose-узел** — ветка «reorder откатывается на неадресуемом
  соседе» сохраняется через синтезируемый paragraph-segment (text-узел), assertion правится.

**Порядок (каждый: агент → gate → review; C1/C2 — до удаления парсинга, пока YAML ещё парсится):**
- ✅ **C1** (done): удалены 4 `*YamlSnapshot.kt` + `DemoCnlMigrationEquivalenceTest.kt` +
  stale doc-comment в `MissionEventLogSlm.kt`. `:shared:jvmTest` зелёный.
- ✅ **C2** (done, D5): удалён `LegacyMissionOverviewFixture`; 6 тестов перевешены на shipped
  CNL-демо через новый `missionDemoDocuments()` (Overview+Telemetry+EventLog+Shapes); inline-YAML
  в `StructuralWriteBackTest` → CNL; ir-splice-тест → prose-узел (`reorderWithProseSiblingFallsBack`).
  **БОНУС-фича (по решению пользователя):** удаление фикстуры вскрыло, что z-order reorder
  персистился ТОЛЬКО через YAML-скаляр `order:` (нет CNL-фразы) → **реализован section-move reorder**
  (`reorderNodeWriteBack` теперь релоцирует heading-блок через `MoveSection`, как reparent): reorder
  реально пишется в CNL-источник, prose-сосед откатывает в in-memory. `SetTypedBlockScalar order`
  путь в reducer УДАЛЁН. Тесты: `reorderRelocatesSectionOverAddressableRun`,
  `reorderMovesNodeToFrontOfSiblings`, `reorderWithProseSiblingFallsBack`. `:shared:jvmTest` 212 зелёных.
- ✅ **C3** (done): удалена ```ir-цепочка (`IrEscapeHatch`, `parseFence ir`, `SemanticKind.IrSplice`,
  `IrSpliceBlock`, splice-путь `IrNormalizer`, `SlmEditIndex.irSpliceNodes`); ir-тесты удалены/
  перенаправлены на generic unaddressable message. KEEP: `parseDesignNode` (JSON-IR), `FencedCodeBlock`.
- ✅ **C4** (done): `WritePlan`/`TextOp` вынесены в `edit/WritePlan.kt`; `isInteractionExpressibleInSlm`
  переориентирован на CNL-проверку (`CnlGrammar.canRenderInteraction`, тот же name/package → reducer не тронут).
- ✅ **C5** (done): удалены 8 YAML-райтеров (Style/Typography/Network/Motion/Interaction/TypedBlockSet/
  YamlPathWriter/SlmBlockRenderer) + non-CNL routing в `SlmPatcher` (else→`WritePlan.Failed`/in-memory);
  сирот `TypedBlockBinding`/`EditTargetResolver`/`YamlPayload` тоже удалены. Legacy-YAML-узлы → in-memory
  fallback (non-corrupting, по D5). KEEP: `ScalarFormatter`, `CnlWriter`/`SectionWriter`.
- ✅ **C6** (done): удалены `parseTypedGroup`/`typedKeyOf`/`leadingWordColon`/fenced-yaml/near-miss +
  `reservedKeys "ir"`; `ReaderTestSupport.parseEntries` → `parseSlmYaml` напрямую (66 reader-тестов
  сохранены); `TypedBlockDetectionTest` удалён. **31 недомигрированный тест → перенесены НА CNL**
  (покрытие сохранено); 4 удалены как YAML-only-невыразимые (2 IrNormalizer source-map/dup-style +
  explicitPatchOverridingSemanticValueWarns + ComponentTypeWriteBack — последний покрыт
  `AdversarialRoundTripTest.componentTypedNodeRoundTripsAsInTreeFrame`).
  KEEP: `parseSlmYaml`, block-readers, `TypedBlockKind` enum+`fromKey`, `TypedAttributeBlock`/`TypedEntry`.
- ✅ **C7** (done): полный gate по всем модулям BUILD SUCCESSFUL; grep — нулевой YAML/ir-авторинг
  (только CNL-собственный `osaDistance`); демо pure CNL; scratch-тестов нет; KEEP-элементы на месте.

### Открытые follow-up'ы (не блокеры Цели 1)
- Обочинные edge из Block A (в трекере): renderLinks emit order, fill-paint focal ref emit,
  `$`-ref/`fr` grid-track ambiguity.
- Тест-покрытие после C5/C6 удаления (функции работают через CNL tier-3, но нет выделенного CNL-теста):
  `SetNodeRotation` write-back, typography `features`/`axes` write-back, i18n-bundle-regeneration
  под тем же key — добавить как CNL-тесты отдельной задачей.
- Полная compile-валидация ВСЕХ CNL-примеров в доках (greps поймали известные классы).

- **S34 — parse-пути:** из `SlmMarkdownParser` — unfenced typed-блок
  (`parseTypedGroup`/`typedKeyOf`/`leadingWordColon`), fenced-yaml typed, `​```ir`-фенс +
  unfenced-ir-ошибка, near-miss-хинт (`emitNearMissHint`/`isNearMiss`/`osaDistance`). Из
  семантики/нормализатора — `SemanticKind.IrSplice` (+ `SemanticNode.irSplice`,
  `IrSpliceBlock`), splice-путь `IrNormalizer` (`irSpliceNodes`, `splice()`, ir-армы),
  `escape/IrEscapeHatch.kt`, `SlmEditIndex.irSpliceNodes`, ir-арм в `EditTargetResolver`,
  dead `reservedKeys + "ir"`/`fromKey` в `TypedBlockKind`.
- **S35 — YAML-райтеры:** удалить `StyleYamlWriter`, `TypographyYamlWriter`,
  `InteractionYamlWriter`, `MotionYamlWriter`, `NetworkYamlWriter`, `YamlPathWriter`,
  `TypedBlockSetWriter`; убрать вызовы в `SlmPatcher`. `SlmBlockRenderer`/`ScalarFormatter`
  — только если нет вызывающих (grep).
- **S36 — зачистка:** удалить `<Demo>YamlSnapshot`, `LegacyMissionOverviewFixture` (или его
  ir-часть по D5=c), осиротевшие импорты. Полный gate + grep «нулевой YAML/ir-авторинг».

### KEEP (не удалять — это не авторинг)
- `engine.ir.serialization` (внутренний JSON-персист);
- `yaml/SlmYamlParser.kt` (`parseSlmYaml`) — используется frontmatter'ом И десугаром CNL `( )`;
- block-readers + `TypedBlockKind` enum+`.key` + `TypedAttributeBlock`/`TypedEntry` +
  `PatchMerger` — **живой compile-путь CNL** (`CnlElementBlock → cnlNode → desugar →
  TypedEntry → reader → patch → merge`);
- **frontmatter** (`---`-split, `RawYamlBlock`, `FrontmatterReader`, frontmatter-emit в
  `ScreenSourceWriter`) — остаётся YAML;
- `cnlOwners`/`anchorOwners` в `SlmEditIndex` (убираем только `irSpliceNodes`);
- `writeBackEdits`/`withStructuralSource` (формат-независимый in-memory fallback).

---

## Решения от человека (нужны ПЕРЕД Фазой 2 и Фазой 6)

Кратко (полностью — в [2-ПЛАН](2-ПЛАН.md), раздел «Решения»):
- **D1/D2** — для `variables` принято: секции в теле CNL + context-gating; для
  `components` используется lifted `Component:` CNL heading; применить тот же принцип к
  styles или явно пересмотреть.
- **D3** — `axis`/`prop`/`combination`/`set` (def) vs `variant`/`props` (instance) *(реком.)*.
- **D4** — объём G-MASK/G-BIND/G-BOOL: закрыть vs отбросить (ни одному демо не нужны).
- **D5** — `LegacyMissionOverviewFixture`: мигрировать (a) vs удалить/перевесить (c).

## Грубая оценка объёма
- Пункты 1 (Фаза 2) и 3 (миграция всех 4 демо, вкл. S24 `overrides.set`) закрыты. Главный
  остаток: **write-back parity (S30/S31)**, **документация (S32/S33)**, **удаление
  authoring-путей (S34–S36)**.
- Пункт 2 — только поздние parity gaps S25–S29 (weightPerSide, stroke-paint props, rich-text
  spans, `$ref` в literal-слотах, mask.source/boolean children), часть зависит от D4; ни одному
  из 4 демо не нужны.
- Пункты 4–6 — механически, но объёмно (tier-3 CnlWriter, аккуратное удаление с сохранением
  KEEP-списка).
- **Естественный следующий PR:** S30 (tier-3 write-back re-emit + убрать silent
  CNL→YAML fall-through в `SlmPatcher.kt:115`) → S31 (структурные райтеры на `CnlEmitter`).
