# Что сделано

Статус на текущую сессию. Цель — в [1-ЦЕЛЬ](1-ЦЕЛЬ.md), план — в [2-ПЛАН](2-ПЛАН.md),
остаток — в [4-ОСТАЛОСЬ](4-ОСТАЛОСЬ.md).

## ✅ Готова: двусторонняя грамматика CNL (нод-уровень, P0–P10)

Это «грамматическая половина» миссии — **поставлена как PR #12**
(https://github.com/aequicor/mission-visualization/pull/12), ветка
`claude/cnl-support-format-migration-e4f518`, коммит `947a996`
«Make CNL a fully bidirectional authoring format at IR parity».

### Архитектурные краеугольные камни
- **`cnl/CnlGrammar.kt`** — реестр `Descriptor(kind, keyword, order, render)` — единый
  источник истины для парсинга И генерации. **70 дескрипторов**, рендереры kind-gated.
- **`cnl/CnlEmitter.kt`** — детерминированный IR→CNL: `emitSentence`, `emitHeadingSuffix`,
  `emitHeadingLine`, `emitSubtree`, `emitDocumentBody` (регенерирует тело каждого экрана),
  `emitDocument` (standalone `.layout.md`: frontmatter + словари + тело).
  Отдельные document-scope эмиттеры: `emitVariables`, `emitComponents`, `emitStyles`.
  `phrasesOf` растёт автоматически с реестром.
- **`cnl/CnlVocabulary.kt`** — English-only: существительные + **82 keyword→kind** (lowercase) +
  booleans. RU-синонимы убраны.
- **`cnl/CnlParser.kt`** — токенайзер с `( )`-структурой + рекурсивный `parseGroup`
  (`GNode`/`GLeaf`/`GGroup`); `BlockBuilder` c аккумуляторами → `toEntries()`.
- **`cnl/CnlModel.kt`** — `CnlPropertyKind` покрывает все бакеты.
- **`( )`-группы со scope-локализацией** — под-ключи резолвятся локальной таблицей бакета,
  не утекают на уровень узла (keystone против утечки per-fill `opacity`/`blend` в node style).

### Покрытые бакеты (все зелёные)
| Бакет | Что умеет (обе стороны) |
|-------|-------------------------|
| **P0** geometry/core | noun, `120 by 15`, radius/opacity/rotation, layout mode/gap/padding, parent-align, font size/weight (bold/semibold/thin) |
| **P1** fills | multi-fill, `gradient (linear from(x y) to(x y) stops(#a at 0)…)`, `image (asset «..» crop focus(x y) replaceable)`, `video (… autoplay loop muted no)`, per-fill `color (#hex opacity blend visible no)` |
| **P2** corners | per-corner `radius (tl tr br bl)`, `smoothing N`, `blend <mode>`, `styles (fill/text/effect/grid)` |
| **P3** strokes/effects | `stroke (color … weight N align outside dash (4 2) cap round join bevel)`, `effect (dropShadow/innerShadow/layerBlur/backgroundBlur …)` |
| **P4a** layout-deep | width/height modes (`fill`/`hug`/`(fixed 200 min 100)`), `align (…)`, `distribute`/`justify`, `wrap`/`clip`/`absolute`, `anchor (…)`, `constraints (…)`, `gap auto`, `gap (row 8 column 12)` |
| **P4b** grid | `columns`/`rows`/`place`/`guides`/`grids`/`overflow`/`scroll` (гетерогенные треки — reader-gap → null) |
| **P5** typography | font/line-height/tracking/paragraph-spacing/text-align/valign/case/decoration/features/axes/autosize/truncate/maxLines/list/text-style (emphasis-спаны отложены — reader-gap) |
| **P6** media/vector | media-record, shape points/inner, viewBox, icon/svg/path, vector network, boolean op, mask |
| **P7** interactions/motion | 9 триггеров / 10 действий / transitions+spring / overlay + motion-пресеты |
| **P8** components (instance) | `of` ref / variant / props / `$prop` / detach / reset / slot / nested overrides |
| **P9** data-bindings | `$ref`/`{{expr}}` в fill/opacity |
| **P10** responsive | `when (dim value) …` + export + handoff |

### `emitDocument` / `emitDocumentBody`
`CnlEmitter.emitDocumentBody(document, includeId=true)` регенерирует markdown-дерево
заголовков + одно предложение на узел для каждого экрана.
`CnlEmitter.emitDocument(document, includeId=true)` теперь является единым SLM-emitter'ом:
YAML frontmatter (оставшийся runtime metadata слой) + CNL `Collection`/`Styles` sections +
body + lifted `Component:` definitions. Это база для миграции демо (Фаза 3).

### Тесты
- **443 frontend + 201 shared** — зелёные.
- `CnlEmitterRoundTripTest` — **80 round-trip кейсов** (`parse(emit(node)) ≡ node`) +
  `assertSameFields` по всем бакетам.
- `CnlGrammarSymmetryTest` — эмиттерный keyword ⊆ словаря парсера.
- `CnlDocumentRoundTripTest` — регенерация body + идемпотентность `emitDocumentBody`.
- 5 старых CNL-тестов переписаны на English.

## ✅ Готова: архитектурная разведка + упорядоченный план (эта сессия)

Read-only воркфлоу (`wf_341370fe-f34`, 4 маппера + синтез, 5 агентов, 0 ошибок) выдал
верифицированную по коду карту и упорядоченный, гейтованный план S1–S36 — он лёг в основу
[2-ПЛАН](2-ПЛАН.md). Разведка подтвердила по коду (file:line):
- **document-scoped авторинг** — как `variables:`/`component:`/`styles` авторятся сегодня и
  ложатся в `DesignDocument.{variables,components,styles}` (readers, patches, lifters);
- **аудит 4 демо + LegacyFixture** — какие словари и ir-only фичи использует каждое
  (детали — в [4-ОСТАЛОСЬ](4-ОСТАЛОСЬ.md));
- **каталог reader-gap'ов** — 14 позиций с точным IR-полем, текущим поведением и требуемой
  правкой;
- **карта удаления** — 8 YAML-райтеров, все authoring-parse-ветки, routing `SlmPatcher`/
  `cnlOwners`/`isCnlElement`, и что именно оставить.

## ✅ Инфраструктурно
- **Baseline зелёный:** полный gate проходит (`:engine:ir` + `:engine:frontend` + `:shared`
  тесты + `:desktopApp:compileKotlin`).
- Память проекта обновлена: `memory/cnl-authoring.md`.

## ✅ Готова: Фаза 0 + нодовые gaps S7/S8/S8b (текущая сессия)

Закрыт естественный следующий PR-блок из [2-ПЛАН](2-ПЛАН.md): emitter-only gaps и
нод-level reader gaps, которые не требуют решений D1–D5.

### Закрытые gaps
- **S1 / G-TEXTKEY:** `TextContent.key` теперь эмитится как `key <id>` и round-trip'ится.
- **S2 / G-WEIGHT:** произвольный вес шрифта эмитится/парсится как `weight N`
  (`bold`/`semibold`/`thin` остались каноническими короткими формами).
- **S3 / G-ADDL:** `visible` и `locked` теперь CNL-фразы (`visible no`, `locked yes`);
  `visible` поддерживает `$prop.*`/`$token`/`{{expr}}`.
- **S4 / G-GRIDTRACKS:** гетерогенные `columns`/`rows` выражаются как
  `tracks (...)`, например `columns (tracks (1fr 2fr hug))`.
- **S5 / G-IMPLICITROWS:** implicit rows теперь могут иметь не-`1fr` трек:
  `rows (auto track hug min 96)`.
- **S6 / G-MOTIONREF:** ref-less inline motion теперь выражается без пустого ref:
  `motion duration 800 loop frames (...)`.
- **S7 / G-STROKESTYLE + G-STICKY:** `styles (stroke <id>)` читает/эмитит
  `strokeStyleId`; `scroll (... sticky)` читает/эмитит `DesignScroll.sticky`.
- **S8 / G-VARMODES:** per-node `variableModes` выражаются как
  `modes (theme dark density compact)`.
- **S8b / часть G-OVR:** prop-bound raw text characters и node-body `$prop`-binding
  закрыты: `characters $prop.title`, `visible $prop.showTail`.

### Тесты
- `CnlEmitterRoundTripTest` расширен проверками `visible`, `locked`, style refs,
  `variableModes`, `content.key`, raw `characters`, heterogeneous tracks, implicit rows,
  ref-less motion и `$prop` bindings.
- Полный gate зелёный:
  `./gradlew :engine:ir:jvmTest :engine:frontend:jvmTest :shared:jvmTest :desktopApp:compileKotlin`.

## ✅ Готова: S9–S12 document-scoped CNL для variables (текущая сессия)

Практически зафиксированы решения **D1/D2** для первого document-scope словаря:
служебные CNL-секции в теле markdown + context-gating по заголовку.

### Что добавлено
- **S10:** `SemanticScreen.documentPatches` + ветка в `SemanticExtractor`: заголовки
  `# Collection ...` и `# Prototype Variables` потребляются как document-scope секции,
  не создают visible nodes и не меняют heading stack.
- **S11:** CNL parse-in для variable collections и prototype variables:
  `# Collection theme (modes light dark default light)`,
  строки `Color/Number/String/Boolean <id> <mode> <value> ...`, alias `$token`,
  boolean `yes/no`.
- **S12:** `CnlEmitter.emitVariables(document)` — deterministic IR→CNL для
  `DesignDocument.variables.collections` и `prototypeVariables`.
- Десугар идёт в существующий путь `TypedEntry(Variables)` →
  `VariablesBlockReader` → `VariablesPatch` → `liftDocumentPatches`, без отдельного
  второго normalizer.

### Тесты
- `CnlVariablesDocumentTest` проверяет collections, prototype variables, alias values,
  отсутствие phantom nodes и `emitVariables` fixed point.
- Полный gate зелёный:
  `./gradlew :engine:ir:jvmTest :engine:frontend:jvmTest :shared:jvmTest :desktopApp:compileKotlin`.

## ✅ Готова: S13–S14 component definitions CNL parse/emit (текущая сессия)

Component definitions теперь можно авторить без YAML `node:`/`component:` metadata.
Сохранён существующий compile-путь: `Component:`-marked subtree материализуется как
обычная root-нода компонента, затем `ComponentLifter` поднимает её в
`DesignDocument.components/componentSets`.

### Что добавлено
- `Component:` heading теперь самодостаточен: extractor добавляет semantic
  `node.type=component`, поэтому `node: { type: component }` больше не нужен.
- CNL definition metadata на heading:
  - `component-name «Log Row»`
  - `set cmpWireTileSet`
  - `axis kind (default highlight)`
  - существующий `variant (kind default/highlight)`
  - `prop label (text default «Event»)`, `prop actions (slot min 0 max 3)`.
- `CnlParser.parseHeading` теперь валидирует candidate suffix через `parse + desugar`;
  это убирает ложный split на словах вроде `Row` в названии компонента.
- `CnlEmitter.emitComponents(document)` регенерирует lifted component definitions как
  `## Component:` CNL-секции: root id, component metadata, axes/variant/property defs и
  CNL subtree children.

### Тесты
- `CnlComponentsDocumentTest` проверяет lift из visible tree, component sets, prop defs,
  instance ref resolution, resolver variant expansion и fixed point `emitComponents`.
- `CnlComponentsDocumentTest` также покрывает generated i18n `TextContent` для text props.
- Полный gate зелёный:
  `./gradlew :engine:ir:jvmTest :engine:frontend:jvmTest :shared:jvmTest :desktopApp:compileKotlin`.

## ✅ Готова: S15–S16 shared styles CNL parse/lift/emit (текущая сессия)

Shared styles теперь авторятся как document-scope CNL-секция, без YAML `styles:`:

```text
# Styles

Paint color.surface color #FFFFFF
TextStyle type.body font Inter size 16 weight 500 line-height 24
Effect shadow.card effect (dropShadow color #000000 opacity 0.25 offset (0 4) blur 16)
Grid layout.desktop grids (columns count 12 gutter 16 margin 80)
```

### Что добавлено
- `TypedBlockKind.Styles` + `StylesPatch` + `readStylesBlock`: новый typed-reader путь
  в `DesignStyle.Paint/Text/Effect/Grid`.
- `SemanticExtractor` теперь потребляет `# Styles` как document-scope section; строки
  `Paint`/`TextStyle`/`Effect`/`Grid` не становятся visible nodes.
- `IrNormalizer.liftDocumentPatches` поднимает `StylesPatch` в `DesignDocument.styles`.
- Parse-in переиспользует существующий CNL desugar node-фраз (`color`, `font`, `effect`,
  `grids`) вместо второго словаря свойств.
- `CnlEmitter.emitStyles(document)` регенерирует styles через обычный `CnlGrammar`:
  style превращается во временный IR-узел, после чего берутся те же canonical phrases.

### Тесты
- `CnlSharedStylesDocumentTest` проверяет:
  - `# Styles` → `document.styles`;
  - отсутствие phantom node `Styles`;
  - resolver применяет `fill/effect/grid/text` shared style refs к CNL-узлам;
  - `emitStyles` fixed point после recompile.
- Полный gate зелёный:
  `./gradlew :engine:ir:jvmTest :engine:frontend:jvmTest :shared:jvmTest :desktopApp:compileKotlin`.

## ✅ Готова: S17–S18 aggregate cross-check + единый document emit (текущая сессия)

Document-scoped CNL теперь собран в один deterministic emitter:

- `emitDocumentBody(document)` — body-only строительный блок для heading tree.
- `emitFrontmatter(document)` — retained YAML frontmatter из `DesignDocument.screen`,
  `i18n`, `breakpoints`, `libraries`.
- `emitDocument(document)` — standalone `.layout.md`: frontmatter → variables →
  styles → body → component definitions.

### Что проверено
- Aggregate source с `Collection`, `Styles`, `Component:`, body nodes и ссылками
  `styles (fill ...)`, `text-style`, `Instance of ...`, `$prop.*`.
- Recompile emitted full document сохраняет screen metadata, locales, breakpoints,
  libraries, variables, prototype variables, styles, component keys/properties и node ids.
- Resolver после recompile применяет shared paint/text styles и раскрывает component
  instance props.
- `emitDocument` fixed point: `emit → compile → emit` стабилен.

### Тесты
- `CnlFullDocumentEmitTest`.
- Focused frontend gate зелёный: `./gradlew :engine:frontend:jvmTest`.

## ✅ Готова: S20 ShapesShowcase migrated to CNL (текущая сессия)

Первое YAML-демо мигрировано emitter-driven способом и проверено на IR parity:

- `ShapesShowcaseSlm` больше не содержит YAML typed-block authoring и `ir` fences; shipped
  source теперь CNL + retained YAML frontmatter.
- Старый YAML-источник сохранён как временный migration fixture
  `ShapesShowcaseYamlSnapshot` в `commonTest`.
- `DemoCnlMigrationEquivalenceTest` компилирует YAML snapshot и новый CNL source и
  сравнивает `DesignDocument` после нормализации только source-map координат.

### CNL gaps, закрытые по дороге к exact parity
- Container headings теперь могут быть typed: `Frame: Card`, `Vector: Union`; extractor
  lower'ит prefix через CNL nouns в `NodePatch(type=...)`.
- Node layer name выражается как `name «Layer Name»`, отдельно от видимого текста и i18n.
- `# Collection theme «Theme» (...)` сохраняет display name коллекции переменных.
- Numeric CNL phrases `gap`, `padding`, `radius` читают/эмитят bindable `$tokens`.
- `emitHeadingLine(..., includeId=true)` сохраняет ids и для container headings; root
  screen heading также эмитит `name`, потому что H1 title и `node.name` — разные поля.

### Тесты
- `DemoCnlMigrationEquivalenceTest`.
- Focused CNL gate зелёный:
  `./gradlew :engine:frontend:jvmTest --tests CnlEmitterRoundTripTest --tests CnlVariablesDocumentTest --tests CnlFullDocumentEmitTest --tests CnlGrammarSymmetryTest`.
- Focused shared migration gate зелёный:
  `./gradlew :shared:jvmTest --tests io.aequicor.visualization.editor.DemoCnlMigrationEquivalenceTest`.

## ✅ Готова: S21 MissionOverview migrated to CNL (текущая сессия)

Самый большой YAML demo source мигрирован тем же emitter-driven способом:

- `MissionOverviewSlm` теперь CNL + retained YAML frontmatter; shipped source не содержит
  YAML typed-block authoring и `ir` fences.
- Старый YAML-источник сохранён как временный migration fixture
  `MissionOverviewYamlSnapshot` в `commonTest`.
- `DemoCnlMigrationEquivalenceTest` теперь проверяет parity для `ShapesShowcase` и
  `MissionOverview`.

### CNL gaps, закрытые по дороге к exact parity
- Full-document body emitter теперь использует stable heading-per-node encoding. Это
  сохраняет ordered tree, когда leaf sibling идёт после container sibling: markdown heading
  scopes не имеют закрывающего маркера, поэтому compact leaf sentence там неоднозначна.
- Stable headings стали prefix-only (`Text:`, `Rectangle:`) + явное `name «... »`, чтобы
  layer names со словами вроде `Name`, `Icon`, `Size` не split'ились как CNL keywords.
- `CnlParser.parseHeading` стал type-aware для typed prefixes: несовместимый suffix
  (`Shape: Layer Icon 0` → `vector.iconRef`) отклоняется.
- `characters «literal»` в heading suffix теперь lowering'ится в `text.defaultText`;
  `$prop`/`{{expr}}` остаются raw `characters` bindings.
- Text nodes снова эмитят fixed/hug box sizing (`74 by 40`, `width hug height hug`)
  отдельно от font size (`size 14`).

### Тесты
- `DemoCnlMigrationEquivalenceTest`.
- Focused CNL gate зелёный:
  `./gradlew :engine:frontend:jvmTest --tests CnlDocumentRoundTripTest --tests CnlEmitterRoundTripTest --tests CnlFullDocumentEmitTest --tests CnlComponentsDocumentTest`.
- Focused shared migration gate зелёный:
  `./gradlew :shared:jvmTest --tests io.aequicor.visualization.editor.DemoCnlMigrationEquivalenceTest`.

## ✅ Готова: S22 MissionTelemetry migrated to CNL (текущая сессия)

Третье YAML demo source мигрировано на CNL:

- `MissionTelemetrySlm` теперь CNL + retained YAML frontmatter; shipped source не содержит
  YAML typed-block authoring и `ir` fences.
- Старый YAML-источник сохранён как временный migration fixture
  `MissionTelemetryYamlSnapshot` в `commonTest`.
- `DemoCnlMigrationEquivalenceTest` теперь проверяет parity для `ShapesShowcase`,
  `MissionOverview` и `MissionTelemetry`.

### CNL gaps, закрытые по дороге к exact parity
- `fill`/`hug` sizing axes теперь сохраняют optional preferred value через record-форму:
  `width (fill 416)`, `width (hug 88)`, `height (hug 28)`.
- `CnlEmitter.renderAxis` больше не теряет `DesignSize` для non-fixed sizing modes с
  numeric value; parser уже читает эту форму через sizing record.
- MissionTelemetry component definitions (`cmp_wire_tile_default/highlight`) и live badge
  теперь round-trip'ят с теми же `DesignSize` + `DesignSizing`, что YAML snapshot.
- Heading-authored CNL nodes теперь попадают в `SlmEditIndex.cnlOwners`; typography
  write-back для таких строк патчит CNL-фразы (`size`, `line-height`, `tracking`,
  `text-align`) вместо падения в YAML typed-block fallback.

### Тесты
- `DemoCnlMigrationEquivalenceTest`.
- Focused CNL gate зелёный:
  `./gradlew :engine:frontend:jvmTest --tests CnlDocumentRoundTripTest --tests CnlEmitterRoundTripTest --tests CnlFullDocumentEmitTest --tests CnlComponentsDocumentTest`.
- Focused shared migration gate зелёный:
  `./gradlew :shared:jvmTest --tests io.aequicor.visualization.editor.DemoCnlMigrationEquivalenceTest`.
- Полный gate зелёный:
  `./gradlew :engine:ir:jvmTest :engine:frontend:jvmTest :shared:jvmTest :desktopApp:compileKotlin`.

## Что это даёт
Способность «**генерировать**» (IR→CNL) на уровне узлов готова и проверена. Способность
«**понимать**» работает. «**Обновлять**» — работает хирургически для части правок (span-replace +
phrase-append), но пока падает в YAML-fallback для остального (это чинит S30). Не хватает
для полного отказа от YAML — **несколько reader-gap'ов**, **миграция демо + write-back parity
+ удаление** — см. [4-ОСТАЛОСЬ](4-ОСТАЛОСЬ.md).
