# Осталось

- Незавершённого по согласованному объёму PR нет.
- Ограничения v1 остаются осознанными: embedded image вместо freehand-ink, plain text body, per-screen node ids, export в clipboard без отдельного download-файла.
- Полный локальный `./gradlew build` не завершился из-за Kotlin/Native `:shared:linkReleaseFrameworkIosSimulatorArm64` / `Java heap space` при 4 GiB heap; целевой JVM/JS/Wasm/Desktop/Web/Android набор прошёл.
