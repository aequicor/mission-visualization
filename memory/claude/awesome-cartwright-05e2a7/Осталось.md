# Осталось

- Незавершённого по согласованному объёму PR нет (включая production-hardening pass).
- Осознанные остаточные ограничения v1: embedded image вместо freehand-ink (вставка data-URI,
  файл-пикера в редакторе нет), plain text body (задел на RichText из typography).
- Полный локальный `./gradlew build` не завершился из-за Kotlin/Native
  `:shared:linkReleaseFrameworkIosSimulatorArm64` / `Java heap space` при 4 GiB heap;
  целевой JVM/JS/Wasm/Desktop/Web/Android набор прошёл (после hardening дополнительно
  прошёл `:shared:compileKotlinIosArm64`).
